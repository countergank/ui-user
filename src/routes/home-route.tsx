import { HomePage } from "@/features/home/home-page";

/**
 * Lazy-loaded route module for "/" (ARCH-5, bundle-001).
 * Loaded on demand via the router's `lazy` -> dynamic import.
 */
export function Component() {
  return <HomePage />;
}
